import 'package:flutter/material.dart';

class Discount10PercentageScreen extends StatelessWidget {
  const Discount10PercentageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Discount 10%')));
  }
}
