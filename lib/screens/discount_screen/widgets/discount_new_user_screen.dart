import 'package:flutter/material.dart';

class DiscountNewUserScreen extends StatelessWidget {
  const DiscountNewUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Discount new user')));
  }
}
