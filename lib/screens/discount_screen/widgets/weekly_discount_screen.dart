import 'package:flutter/material.dart';

class WeeklyDiscountScreen extends StatelessWidget {
  const WeeklyDiscountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Weekly discount')));
  }
}
