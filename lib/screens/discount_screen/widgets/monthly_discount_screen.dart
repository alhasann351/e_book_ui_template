import 'package:flutter/material.dart';

class MonthlyDiscountScreen extends StatelessWidget {
  const MonthlyDiscountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Monthly discount')));
  }
}
