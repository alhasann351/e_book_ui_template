import 'package:flutter/material.dart';

class NewBookDiscountScreen extends StatelessWidget {
  const NewBookDiscountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('New book discount')));
  }
}
